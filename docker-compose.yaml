
services:
  backend:
    build: .
    container_name: backend
    command: python manage.py runserver --settings=core.setting.dev 0.0.0.0:8000
    volumes:
      - ./RoomAccounting:/app

    ports:
      - "8000:8000"
    
    environment:
      - SECRET_KEY=test
